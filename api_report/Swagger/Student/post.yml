Add new student into db
---
tags:
  - Student
parameters:
  - name: student_id
    in: path
    required: true
    type: integer
  - name: Student info
    in: body
    schema:
      $ref: "#definitions/put_student"
    responses:
      200:
        description: Add new student into db
produces:
  - application/json
responses:
  200:
    description: Update student info
