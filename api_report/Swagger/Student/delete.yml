Delete student from db
---
tags:
  - Student
parameters:
  - name: student_id
    in: path
    required: true
    type: integer
responses:
  200:
    description: Delete student from db
    schema:
      $ref: '#/definitions/student'
produces:
  - application/json