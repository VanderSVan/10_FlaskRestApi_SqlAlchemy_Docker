Get group list
---
tags:
  - Groups
parameters:
  - name: student_count
    in: query
    required: false
    type: integer
    description: Finds all groups with less or equals student count
responses:
  200:
    description: Get group list with info
    schema:
      $ref: '#/definitions/groups'
produces:
  - application/json