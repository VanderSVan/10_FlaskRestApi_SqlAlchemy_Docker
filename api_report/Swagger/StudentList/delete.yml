Delete student list from db
---
tags:
  - Student list
parameters:
  - name: student_id_list
    in: query
    required: true
    type: string
responses:
  200:
    description: Delete students from db
    schema:
      $ref: '#/definitions/student'
produces:
  - application/json