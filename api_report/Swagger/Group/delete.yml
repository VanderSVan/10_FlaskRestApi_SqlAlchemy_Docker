Delete group from db
---
tags:
  - Group
parameters:
  - name: group_id
    in: path
    required: true
    type: integer
responses:
  200:
    description: Delete group from db
    schema:
      $ref: '#/definitions/group'
produces:
  - application/json
definitions:
  group:
    type: object
    properties:
      group_id:
        type: string
      name:
        type: string
    example:
      group_id: 1
      name: Mathematics
