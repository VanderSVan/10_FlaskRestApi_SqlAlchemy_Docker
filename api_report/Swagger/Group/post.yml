Add new group into db
---
tags:
  - Group
parameters:
  - name: group_id
    in: path
    required: true
    type: integer
  - name: Group info
    in: body
    schema:
      $ref: "#definitions/post_group"

responses:
  200:
    description: Add new group into db
    schema:
      $ref: '#/definitions/post_group'
produces:
  - application/json

definitions:
  post_group:
      type: object
      properties:
        name:
          type: string
        students:
          type: array
          items:
            properties:
              student_id:
                type: integer
      example:
        name: XY-12
        students: [14, 19, 23]
